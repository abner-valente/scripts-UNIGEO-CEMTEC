# Dependências do explorador (app/explorador.py).
#
# Fica nesta pasta, e não na raiz, porque é aqui que o Streamlit Community Cloud procura as
# dependências do arquivo publicado.
#
# A aba do mapa usa o mesmo modulos/mapas.py dos produtos — mesma interpolação, mesmo recorte
# pelo estado, mesmas cores. Por isso geopandas, shapely, pyogrio, pyproj e scipy voltaram para
# cá: são ~165 MB a mais na publicação, o preço de não ter um segundo desenho de mapa para
# manter. Se um dia isso pesar demais, o caminho é converter os shapefiles para GeoJSON e fazer
# o IDW em numpy puro — mas aí a tela e o relatório podem divergir com o tempo.

streamlit>=1.50
altair>=5.0
pydeck>=0.9   # mapa navegável; já vem com o streamlit, declarado aqui porque o app o importa
pandas>=2.0
numpy>=1.24
matplotlib>=3.5
requests>=2.28
python-dotenv>=1.0
tzdata

# Mapas (compartilhados com os produtos)
geopandas>=1.0
shapely>=2.0
pyogrio>=0.7
pyproj>=3.5
scipy>=1.10
